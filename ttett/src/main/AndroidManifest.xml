<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ttett">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".OpenFolderActivity"></activity>
        <activity android:name=".UserinfoActivity" />
        <activity android:name=".MailDialogActivity" />
        <activity android:name=".selectAcitvity.SelectAttachmentActivity" />
        <activity
            android:name=".selectAcitvity.SelectMailActivity"
            android:theme="@style/Anim_fade" />
        <activity android:name=".LoginEmailActivity" />
        <activity android:name=".Contact_module.SimpleContactActivity" />
        <activity android:name=".RegisteredActivity" />
        <activity android:name=".OpenMailActivity" />
        <activity
            android:name=".WriteLetterActivity"
            android:theme="@style/Anim_fade" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Anim_fade" />
        <activity android:name=".Contact_module.ContactsActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>